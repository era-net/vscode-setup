To import the configuration download `general.code-profile` onto the machine you'd like to setup.

In VSCode press `CTRL` + `SHIFT` + `P` and type `import`.

Choose `general.code-profile` you just downloaded.

Done!
